server:
  port: 8080
  address: 0.0.0.0

spring:
  application:
    name: agriapp-backend
  profiles:
    default: mock

spring.main.banner-mode: off

logging:
  level:
    root: INFO
    com.agriapp: DEBUG